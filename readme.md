# 🎵 1mpu1se Music Streaming - Search Infrastructure

Полная копия продакшен-инфраструктуры стримингового сервиса 1mpu1se с расширенным поисковым функционалом.

## 🏗️ Архитектура сервисов

### 🗄️ Базы данных и хранилища
- **🐘 PostgreSQL** (`5432`) - основная реляционная БД
- **🔴 Redis** (`6379`) - кэширование и сессии
- **📦 MinIO** (`9000`) - S3-совместимое хранилище
  
### 🔍 Поисковый стек (НОВОЕ 🚀)
- **🔍 Elasticsearch** (`9200`) - поисковый движок
- **🐍 Search Service** (`8000`) - Python микросервис
## 🚀 Быстрый старт

```bash
# Запуск всей инфраструктуры
docker-compose up -d

# Проверка работы
curl http://localhost:9200          # Elasticsearch
curl http://localhost:8000/health   # Search Service

# Или всё одной командой
docker-compose up -d && sleep 30 && curl http://localhost:8000
